import UIKit

// Intentionally empty.
//
// This file is still referenced by the Xcode project, but the app uses
// the classic AppDelegate-based lifecycle (see AppDelegate.swift) — the
// UIApplicationSceneManifest has been removed from Info.plist, so iOS
// never instantiates a scene delegate. We keep an empty, type-free file
// here so it compiles on Flutter 3.19 (which has no FlutterSceneDelegate)
// without having to surgically edit the .pbxproj file references.
